{
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo  run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.10.12"
  }
}
