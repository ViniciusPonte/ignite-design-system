{
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo  run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=@vponte-ignite/docs && changeset publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "turbo": "^1.10.12"
  }
}
